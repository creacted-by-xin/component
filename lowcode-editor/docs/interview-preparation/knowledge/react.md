# React 与状态

## 组件树更新的基本原则

- state 更新应返回新引用，不应把既有嵌套节点原地修改后假设视图总能正确刷新。
- 对树状 Schema，优先将查找、插入、删除和更新抽成可测试的纯函数；历史栈、导入导出和 AI patch 都能复用它们。
- Hook 必须在每次渲染按相同顺序调用，不能放到条件 return 之后。

**项目映射**：当前 store 的组件树直接修改嵌套 `children`，且部分 dev renderer 有条件 Hook；这只是审计发现，不代表用户已掌握该主题。
