# 错题与未掌握项

| ID | 关联题目 | 类型 | 当前表现/原因 | 纠正要点 | 复练入口 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| `M-001` | `B-01` | 概念不完整 | 已说明作用域和 `var` 提升；未说明 `let`/`const` 的 TDZ 与 `const` 重新赋值边界 | 区分“绑定创建”与“可读取”；用 `ReferenceError`、初始化、重新赋值三个关键词回答 | Day 1 开场口述 | `OPEN` |
| `M-002` | `B-02` | 表述不精确 | 输出正确；将宏任务描述为“每次清空当前队列” | 当前任务结束后清空微任务；再由事件循环选择下一个任务，timer 不保证精确时刻执行 | Day 1 异步小测 | `OPEN` |
| `M-003` | `B-03` | 未掌握 | 未能解释 React state 不可直接修改 | 从“旧引用、React 更新判断、不可变树操作、历史栈”建立理解 | Day 1 必做 | `OPEN` |
| `M-004` | `B-04` | 未掌握 | 未能区分 `unknown`、`any` 与联合类型 | 建立“未知输入先 unknown+收窄；有限可能用联合；避免 any”规则 | Day 1 必做 | `OPEN` |
| `M-005` | `B-05` | 未掌握 | 尚不能描述物料到预览的组件树数据流 | 用“注册表 -> 拖拽 item -> Zustand 组件树 -> renderer -> action handler”复述 | Day 1 项目任务 | `OPEN` |
| `M-006` | `C-BL-001` | 未掌握 | 尚未能写递归树查找 | 先完成一层循环，再在 `children` 上递归；覆盖空树和未找到 | Day 1 手写 | `OPEN` |

未作答按“未掌握/待补”记录，不等同于能力定型；复练通过后保留历史并更新状态。
